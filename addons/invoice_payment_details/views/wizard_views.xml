<odoo>
    <record id="view_payment_history_wizard_form" model="ir.ui.view">
        <field name="name">payment.history.wizard.form</field>
        <field name="model">payment.history.wizard</field>
        <field name="arch" type="xml">
            <form string="Payment History">
                <sheet>
                    <field name="invoice_id" readonly="1"/>
                    <field name="payment_ids">
                        <tree>
                            <field name="date"/>
                            <field name="amount"/>
                            <field name="journal_id"/>
                            <field name="name"/>
                        </tree>
                    </field>
                </sheet>
                <footer>
                    <button string="Close" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_payment_history_wizard" model="ir.actions.act_window">
        <field name="name">Payment History</field>
        <field name="res_model">payment.history.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
