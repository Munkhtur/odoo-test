<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Inherit ONLY Customer Invoices tree -->
        <record id="view_out_invoice_tree_paid" model="ir.ui.view">
            <field name="name">account.move.out_invoice.tree.paid</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_out_invoice_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='amount_total_signed']" position="after">
                    <field name="amount_paid"
                           string="Төлөгдсөн дүн"
                           widget="monetary"
                           options="{'currency_field': 'currency_id'}"/>
                    <button name="action_open_payment_history"
                            type="object"
                            string="Дэлгэрэнгүй"
                            icon="fa-history"
                            groups="account.group_account_manager"/>
                </xpath>

          
          
            </field>
        </record>

    </data>
</odoo>